# 工作流索引

AI agent 处理本资料库任务时，应先阅读根目录 `AGENTS.md`，再阅读本索引，并根据任务触发条件阅读对应的 active 工作流。

## 使用规则

- 工作流用于规定可重复执行的资料库维护步骤。
- 流程、维护说明和可重复执行的规则默认放在 `workflows/`；未经用户明确要求，不新建 `docs/` 目录。
- 工作流不得覆盖 `AGENTS.md` 中关于来源、ID、大纲、索引和结构检查的硬约束。
- 当任务涉及多个工作流时，先执行更基础的流程，再执行更具体的流程。
- 若现有工作流缺失、过时或无法覆盖正在执行的重复性任务，AI agent 应主动提出并增量更新工作流。

## 当前工作流

- `WF-001`：工作流维护，见 `workflows/WF-001-工作流维护.md`。
- `WF-002`：做题反馈入库，见 `workflows/WF-002-做题反馈入库.md`。
- `WF-003`：Git版本管理，见 `workflows/WF-003-Git版本管理.md`。

## 更新后检查

新增或修改 `workflows/` 下的工作流文件后，必须运行结构检查脚本。
