---
id: "WF-003"
title: "Git版本管理"
status: active
applies_to: "检查仓库状态、提交变更、尝试推送或报告推送阻塞原因"
triggers: ["用户要求提交commit", "用户要求检查git状态", "用户要求推送远程仓库", "完成一组可独立保存的资料库变更"]
last_updated: "2026-06-24"
---

# Git版本管理

## 目标

让资料库的重要结构、模板、脚本和内容变更可以被清晰提交，并避免把缓存、大文件或本地状态误提交。

## 触发条件

- 用户要求提交 commit、查看 Git 状态或推送远程仓库。
- 完成一组可独立保存的资料库变更。
- 修改了结构脚本、模板、工作流、知识点或索引。

## 执行步骤

1. 检查仓库根目录、当前分支、远程仓库和工作区状态。
2. 检查 `.gitignore` 是否覆盖缓存、本地状态和不应提交的大文件。
3. 运行结构检查脚本；若修改知识点元数据，先重新生成索引。
4. 查看将要提交的文件清单，确认没有 PDF、缓存或本地私有文件。
5. 按可独立理解的变更分批提交，提交信息使用清楚的中文或英文短句。
6. 提交后检查工作区状态。
7. 若配置了远程仓库，尝试推送；若没有远程仓库或权限失败，报告阻塞原因。
8. 若当前变更有必要 commit 保存但 AI agent 因权限、远程配置或其他环境限制无法完成提交，必须在最终回复末尾提醒用户手动 commit，并给出建议提交信息。

## 禁止事项

- 不得为了提交而回滚用户已有改动。
- 不得提交 `.codex/`、`.agents/`、缓存目录或 Python 字节码。
- 不得默认提交大型 PDF 原始资料，除非用户明确要求。
- 不得手工修改自动生成索引来掩盖检查问题。
- 不得在无法完成 commit 时静默结束；必须明确提醒用户保存版本。

## 相关文件

- `.gitignore`
- `scripts/check_structure.py`
- `scripts/build_indexes.py`

## 修订记录

- 2026-06-24：补充无法提交时在最终回复提醒用户手动 commit 的规则。
- 2026-06-24：创建 Git 版本管理工作流。
